import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Modeling Photography | VTCSocial",
  description: "Selected modeling photography by VTCSocial.",
};

export default function ModelingPhotographyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
